// Variáveis
var a = 1.5;
var b = 4;
var c = 2;

// Operações
var bAoQuadrado = Math.pow(b, 2);
var raizQuadrada = Math.sqrt(bAoQuadrado - (4 * a *c));
var calculo = (- b + raizQuadrada) / 2;

alert(calculo);