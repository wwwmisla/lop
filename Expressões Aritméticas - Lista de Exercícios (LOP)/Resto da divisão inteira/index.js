// Receber os valores de entrada em números inteiros
let n1 = Number(prompt('Digite o valor do número 1:'));
let n2 = Number(prompt('Digite o valor do número 2:'));

// Calcular
let calculo = n1 % n2;

// Saída 
alert(calculo);